---
"@quester-studio/cli": patch
---

Desktop ships a Quester app icon/logo, matches the Windows title bar to the app theme (persisted under local app data), and lets Preferences open without a workspace.
