---
"@quester-studio/desktop": patch
---

Collections JSON bodies use a document CodeMirror editor (fold/search/templates); responses can open in a frozen full-bleed tab while the peek panel remains.
