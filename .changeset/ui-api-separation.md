---
"@quester-studio/cli": patch
---

Separate studio UI and backend for local debug: shared `api-contract` / `workspace-service`, `apps/api` (HTTP + SSE), and desktop `dev:web` / `dev:web:mock` modes. Documents setup and debug for each mode. No change to flow format or CLI behavior.
