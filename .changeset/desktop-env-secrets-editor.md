---
"@quester-studio/desktop": patch
---

Improve env and secrets editors with `{{env.*}}` / `{{secrets.*}}` usage hints, a header Save button, masked secret values with show/hide, and template autocomplete for env and secrets keys.
