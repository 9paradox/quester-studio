---
"@quester-studio/desktop": patch
"@quester-studio/schema": patch
---

Inspector Save button, canvas zoom controls, error toasts, logs clear, assert path clear fix, themed zoom controls, and resizable JSON nodes (optional width/height on flow nodes).
