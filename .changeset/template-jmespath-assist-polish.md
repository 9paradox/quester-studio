---
"@quester-studio/desktop": patch
---

Polish Template/JMESPath assist: template Pick path, dual-mode foreach items, JMESPath transform map rows, and per-value template maps for set/output/subflow/form.
