---
"@quester-studio/desktop": patch
---

Desktop canvas polish: per-flow console isolation, drag flows onto the canvas as `subflow` nodes (with sidebar grip affordances), form/code drop stubs, subflow flow picker, multi-select inspector, and align/distribute actions.
