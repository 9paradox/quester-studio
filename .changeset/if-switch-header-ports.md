---
"@quester-studio/desktop": patch
---

Align if/switch outs with try (header-right, auto-grow, live handle refresh) and replace Switch cases JSON with a row editor so canvas ports update as cases change.
