---
"@quester-studio/schema": minor
"@quester-studio/nodes": minor
"@quester-studio/engine": minor
"@quester-studio/cli": minor
---

Forms can declare reusable `inputs` bound per flow via the form node's `bindings` (`{{form.*}}`). Number/json field defaults use template assist; samples no longer hardcode `nodes.*` in shared form files.
