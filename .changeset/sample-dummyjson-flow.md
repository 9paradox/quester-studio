---
"@quester/desktop": patch
"@quester/engine": patch
"@quester/cli": patch
---

Point the sample workspace login flow at dummyjson auth (`emilys` / `emilyspass`) with assert + extract side branches.
