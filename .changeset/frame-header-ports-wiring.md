---
"@quester-studio/schema": patch
"@quester-studio/desktop": patch
---

Frame polish: put `success`/`failed`/`complete` on the header-right, allow only one entry and one exit per frame, reattach exit when chaining body nodes, and reject body sibling cycles on the canvas.
