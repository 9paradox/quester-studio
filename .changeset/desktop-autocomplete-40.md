---
"@quester/desktop": minor
---

Context-aware autocomplete for `{{…}}` templates and JMESPath fields, with nested paths from static node contracts and a learned path-shape cache (`.quester/path-shapes.json`). Includes copy-path from response trees, warn-only stale-path lint, and Eta `it.*` completion on template nodes.
