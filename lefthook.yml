pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,tsx,json,md,mdx,css}"
      run: bunx biome check --no-errors-on-unmatched --staged {staged_files}

pre-push:
  commands:
    test:
      run: bun run test
