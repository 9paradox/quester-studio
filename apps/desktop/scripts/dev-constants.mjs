/** Shared desktop dev-server settings (keep in sync with vite.config.ts). */
export const VITE_DEV_HOST = "127.0.0.1";
export const VITE_DEV_PORT = 5173;
export const VITE_DEV_URL = `http://${VITE_DEV_HOST}:${VITE_DEV_PORT}/`;
