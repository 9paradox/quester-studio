{
	"name": "quester-studio",
	"private": true,
	"workspaces": ["packages/*", "apps/*"],
	"packageManager": "bun@1.3.14",
	"scripts": {
		"build": "bun run build:pkgs; bun run build:apps",
		"build:pkgs": "bun run --filter @quester-studio/schema build; bun run --filter @quester-studio/nodes build; bun run --filter @quester-studio/engine build; bun run --filter @quester-studio/cli build; bun run --filter @quester-studio/api-contract build; bun run --filter @quester-studio/workspace-service build",
		"build:apps": "bun run --filter @quester-studio/web build; bun run --filter @quester-studio/desktop build",
		"test": "bun scripts/workspace-run.mjs test",
		"lint": "biome check .",
		"format": "biome format --write .",
		"typecheck": "bun scripts/workspace-run.mjs typecheck",
		"check": "bun scripts/check.mjs",
		"test:coverage": "bun run --filter @quester-studio/schema test -- --coverage; bun run --filter @quester-studio/nodes test -- --coverage; bun run --filter @quester-studio/engine test -- --coverage; bun run --filter @quester-studio/cli test -- --coverage; bun run --filter @quester-studio/desktop test -- --coverage",
		"changeset": "changeset",
		"version": "changeset version",
		"release:pkgs": "changeset publish",
		"dev": "turbo run dev",
		"turbo:build": "turbo run build",
		"prepare": "lefthook install"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.9",
		"@changesets/cli": "^3.0.1",
		"lefthook": "^2.1.10",
		"turbo": "^2.10.11",
		"typescript": "^7.0.2"
	},
	"overrides": {
		"vite": "6.4.3"
	}
}
